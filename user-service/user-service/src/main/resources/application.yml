server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/user-db
    username: root # DB ??
    password: 0519 # DB ????
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true