server:
  port: 8000

spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            # /users? ???? ?? ??? ??? user-service(localhost:8080)?? ??
            - id: user-service
              uri: http://localhost:8080
              predicates:
                - Path=/users/**
            # /boards? ???? ?? ??? ??? board-service(localhost:8081)?? ??
            - id: board-service
              uri: http://localhost:8081
              predicates:
                - Path=/boards/**
            # /points? ???? ?? ??? ??? point-service(localhost:8082)?? ??
            - id: point-service
              uri: http://localhost:8082
              predicates:
                - Path=/points/**
