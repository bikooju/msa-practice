server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3309/point-db # DB ??
    username: root # DB ??
    password: 0519 # DB ????
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true